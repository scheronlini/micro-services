server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
   config:
    Server:
     git:
      uri:  https://github.com/scheronlini/config-server
      default-label: main
      search-paths:
      - 'greeting*'
